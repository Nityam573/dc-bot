/**
 * Loads a Markdown file and returns its raw text.
 * Markdown syntax is preserved — the chunker handles it as plain text.
 */
export declare function loadMarkdown(filePath: string): string;
//# sourceMappingURL=md.d.ts.map